"use client";

import Verify from "@/components/Verify/Verify";
import { Suspense } from "react";

const VerifyPage = () => {
  return (
    <Suspense>
      <Verify />
    </Suspense>
  );
};

export default VerifyPage;
