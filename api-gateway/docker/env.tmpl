DB_HOST=${DB_HOST}
DB_NAME=${DB_NAME}
DB_PORT=${DB_PORT}
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}

THROTTLER_SHORT_NAME=${THROTTLER_SHORT_NAME}
THROTTLER_MEDIUM_NAME=${THROTTLER_MEDIUM_NAME}
THROTTLER_LONG_NAME=${THROTTLER_LONG_NAME}

THROTTLER_SHORT_TTL=${THROTTLER_SHORT_TTL}
THROTTLER_MEDIUM_TTL=${THROTTLER_MEDIUM_TTL}
THROTTLER_LONG_TTL=${THROTTLER_LONG_TTL}

THROTTLER_SHORT_LIMIT=${THROTTLER_SHORT_LIMIT}
THROTTLER_MEDIUM_LIMIT=${THROTTLER_MEDIUM_LIMIT}
THROTTLER_LONG_LIMIT=${THROTTLER_LONG_LIMIT}

MAIL_HOST=${MAIL_HOST}
MAIL_PORT=${MAIL_PORT}
MAIL_SECURE=${MAIL_SECURE}
MAIL_IGNORE_TLS=${MAIL_IGNORE_TLS}
MAIL_USER=${MAIL_USER}
MAIL_PASSWORD=${MAIL_PASSWORD}
MAIL_SENDER_NAME=${MAIL_SENDER_NAME}
MAIL_VERIFICATION_HOST=${MAIL_VERIFICATION_HOST}

JWT_SECRECT_KEY=${JWT_SECRECT_KEY}
JWT_EXPIRES_IN=${JWT_EXPIRES_IN}

# Google Authentication Enviroment Variables
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
GOOGLE_AUTH_REDIRECT=${GOOGLE_AUTH_REDIRECT}


ADMINISTRATOR_MAIL=${ADMINISTRATOR_MAIL}
ADMINISTRATOR_PASSWORD=${ADMINISTRATOR_PASSWORD}
STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}

RMQ_HOST=${RMQ_HOST}

CONTACT_FORM_MAIL=${CONTACT_FORM_MAIL}

IS_USE_GMAIL=${IS_USE_GMAIL}